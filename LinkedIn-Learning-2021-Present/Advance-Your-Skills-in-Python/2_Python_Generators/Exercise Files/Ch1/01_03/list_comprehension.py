# list comprehension
newlist = [item.upper() for item in collection] # uses square brackets. evaluates to a list

# generator expression
(item.upper() for item in collection) # similar. uses parentheses. evaluates to a generator object
